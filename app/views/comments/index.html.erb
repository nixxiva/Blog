<h2> Comments </h2>
<div>
<% @comments.each do |comment| %>
    <div>
      <p>Title: <%= comment.commenter %></p>
    	<p>Body: <%= comment.body %></p>
			<p> <%= link_to "Show", article_comment_path(@article, comment) %> </p>
			<p> <%= link_to "Edit", edit_article_comment_path(@article, comment) %> </p>
      <p> <%= link_to "Destroy", article_comment_path(@article, comment), 
        data: { turbo_method: :delete, turbo_confirm: "Confirm delete?" } %> </p>
    </div>
    <% end %>
</div>

<p>
  <%= link_to "Back to article", @article %>
</p>
